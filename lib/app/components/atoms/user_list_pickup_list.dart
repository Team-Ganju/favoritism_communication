import 'package:flutter/material.dart';

class UserListPickupList extends StatelessWidget {
  const UserListPickupList({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Spacer();
  }
}
